@model EDInventory.Models.ViewModels.EngAssetViewModel
@{ ViewData["Title"] = "Nuevo Activo Clinico"; }

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0"><i class="bi bi-hospital me-2"></i>Nuevo Activo Clinico</h4>
    <a asp-action="AssetsOnSite" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left me-1"></i>Volver</a>
</div>

<form asp-action="AssetCreate" method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="alert alert-danger small py-2"></div>

    <div class="row g-3">
        <!-- Informacion General -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-info-circle me-1"></i>Informacion General
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-8">
                            <label asp-for="AssetDesc" class="form-label fw-semibold"></label>
                            <input asp-for="AssetDesc" class="form-control" placeholder="Ej: Tomografo Computarizado 64 Cortes" />
                            <span asp-validation-for="AssetDesc" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="LineCode" class="form-label fw-semibold"></label>
                            <select asp-for="LineCode" asp-items="Model.Lines" class="form-select">
                                <option value="">-- Seleccione linea --</option>
                            </select>
                            <span asp-validation-for="LineCode" class="text-danger small"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="ModelCode" class="form-label fw-semibold"></label>
                            <select asp-for="ModelCode" asp-items="Model.Models" class="form-select">
                                <option value="">-- Seleccione modelo --</option>
                            </select>
                        </div>
                        <div class="col-md-3">
                            <label asp-for="AssetSN" class="form-label fw-semibold"></label>
                            <input asp-for="AssetSN" class="form-control" id="snInput" autocomplete="off" />
                            <div id="snFeedback" class="small mt-1"></div>
                        </div>
                        <div class="col-md-3">
                            <label asp-for="AssetNum" class="form-label fw-semibold"></label>
                            <input asp-for="AssetNum" class="form-control" />
                        </div>
                        <div class="col-md-6">
                            <label asp-for="AssetGnum" class="form-label fw-semibold"></label>
                            <input asp-for="AssetGnum" class="form-control" />
                        </div>
                        <div class="col-md-3">
                            <label asp-for="AssetDjequip" class="form-label fw-semibold"></label>
                            <input asp-for="AssetDjequip" class="form-control" type="date" />
                        </div>
                        <div class="col-md-3">
                            <label asp-for="AssetStatus" class="form-label fw-semibold">Estado del Activo</label>
                            <select asp-for="AssetStatus" class="form-select">
                                <option value="EN_SERVICIO">En Servicio</option>
                                <option value="EN_REPARACION">En Reparación</option>
                                <option value="EN_EVALUACION">En Evaluación</option>
                                <option value="DADO_DE_BAJA">Dado de Baja</option>
                            </select>
                        </div>
                        <div class="col-md-3 d-flex align-items-end">
                            <div class="form-check mb-1">
                                <input asp-for="Active" class="form-check-input" type="checkbox" />
                                <label asp-for="Active" class="form-check-label fw-semibold">Activo</label>
                            </div>
                        </div>
                        <div class="col-12">
                            <label asp-for="AssetAddata" class="form-label fw-semibold"></label>
                            <textarea asp-for="AssetAddata" class="form-control" rows="2"></textarea>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Ubicacion -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-geo-alt me-1"></i>Ubicacion
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Tipo de ubicacion</label>
                        <div class="d-flex gap-3">
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="locationType" id="locHospital" value="hospital" checked />
                                <label class="form-check-label" for="locHospital"><i class="bi bi-hospital me-1"></i>Hospital</label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="locationType" id="locBodega" value="bodega" />
                                <label class="form-check-label" for="locBodega"><i class="bi bi-box-seam me-1"></i>Bodega</label>
                            </div>
                            <div class="form-check">
                                <input class="form-check-input" type="radio" name="locationType" id="locSede" value="sede" />
                                <label class="form-check-label" for="locSede"><i class="bi bi-geo-alt me-1"></i>Sede</label>
                            </div>
                        </div>
                    </div>

                    <div id="secHospital" class="row g-3">
                        <div class="col-md-4">
                            <label asp-for="HospCode" class="form-label fw-semibold"></label>
                            <select asp-for="HospCode" asp-items="Model.Hospitals" class="form-select" id="hospSelect">
                                <option value="">-- Seleccione hospital --</option>
                            </select>
                            <span asp-validation-for="HospCode" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="HospDepCode" class="form-label fw-semibold"></label>
                            <select asp-for="HospDepCode" class="form-select" id="hospDepSelect">
                                <option value="">-- Primero seleccione hospital --</option>
                            </select>
                            <span asp-validation-for="HospDepCode" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="AssetHospPos" class="form-label fw-semibold"></label>
                            <input asp-for="AssetHospPos" class="form-control" placeholder="Ej: Sala de Tomografia" />
                            <span asp-validation-for="AssetHospPos" class="text-danger small"></span>
                        </div>
                    </div>

                    <div id="secBodega" class="row g-3 d-none">
                        <div class="col-md-4">
                            <label asp-for="WareCode" class="form-label fw-semibold"></label>
                            <select asp-for="WareCode" asp-items="Model.Warehouses" class="form-select">
                                <option value="">-- Seleccione bodega --</option>
                            </select>
                            <span asp-validation-for="WareCode" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="WareRack" class="form-label fw-semibold"></label>
                            <input asp-for="WareRack" class="form-control" placeholder="Ej: R-01" />
                            <span asp-validation-for="WareRack" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="WareEstante" class="form-label fw-semibold"></label>
                            <input asp-for="WareEstante" class="form-control" placeholder="Ej: E-03" />
                            <span asp-validation-for="WareEstante" class="text-danger small"></span>
                        </div>
                    </div>

                    <div id="secSede" class="row g-3 d-none">
                        <div class="col-md-6">
                            <label asp-for="SiteCode" class="form-label fw-semibold"></label>
                            <select asp-for="SiteCode" asp-items="Model.Sites" class="form-select">
                                <option value="">-- Seleccione sede --</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Conectividad de Red -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-wifi me-1"></i>Conectividad de Red
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label asp-for="NetHostname" class="form-label fw-semibold"></label>
                            <input asp-for="NetHostname" class="form-control" placeholder="Ej: CT-SCANNER-HOSP01" />
                            <span asp-validation-for="NetHostname" class="text-danger small"></span>
                        </div>
                        <div class="col-md-4 d-flex align-items-end pb-1">
                            <div class="form-check">
                                <input asp-for="NetInDomain" class="form-check-input" type="checkbox" />
                                <label asp-for="NetInDomain" class="form-check-label fw-semibold">En Dominio</label>
                            </div>
                        </div>
                        <div class="col-md-4 d-flex align-items-end pb-1">
                            <div class="form-check">
                                <input asp-for="NetEnabled" class="form-check-input" type="checkbox" id="netEnabledCheck" />
                                <label asp-for="NetEnabled" class="form-check-label fw-semibold">Conectado a Red</label>
                            </div>
                        </div>
                        <div id="netDetails" class="col-12 d-none">
                            <div class="row g-3">
                                <div class="col-md-5">
                                    <label asp-for="NetIp" class="form-label fw-semibold"></label>
                                    <input asp-for="NetIp" class="form-control" placeholder="Ej: 10.0.1.50" />
                                    <span asp-validation-for="NetIp" class="text-danger small"></span>
                                </div>
                                <div class="col-md-4 d-flex align-items-end pb-1">
                                    <div>
                                        <div class="fw-semibold mb-1 small">Tipo IP</div>
                                        <div class="d-flex gap-3">
                                            <div class="form-check">
                                                <input class="form-check-input" type="radio" name="NetType" value="DHCP" id="netDhcp" checked />
                                                <label class="form-check-label" for="netDhcp">DHCP</label>
                                            </div>
                                            <div class="form-check">
                                                <input class="form-check-input" type="radio" name="NetType" value="STATIC" id="netStatic" />
                                                <label class="form-check-label" for="netStatic">Static</label>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Responsable / Costo -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-person-badge me-1"></i>Responsable y Costo
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label asp-for="ResponsibleUser" class="form-label fw-semibold"></label>
                            <select asp-for="ResponsibleUser" asp-items="Model.TechUsers" class="form-select">
                                <option value="">-- Sin responsable interno --</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="ResponsibleExt" class="form-label fw-semibold"></label>
                            <input asp-for="ResponsibleExt" class="form-control" placeholder="Nombre del contacto externo" />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="AcquireCost" class="form-label fw-semibold"></label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input asp-for="AcquireCost" class="form-control" type="number" step="0.01" min="0" placeholder="0.00" />
                            </div>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="AcquireDate" class="form-label fw-semibold"></label>
                            <input asp-for="AcquireDate" class="form-control" type="date" />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="DepreYears" class="form-label fw-semibold"></label>
                            <input asp-for="DepreYears" class="form-control" type="number" min="1" max="50" placeholder="Ej: 5" />
                            <span asp-validation-for="DepreYears" class="text-danger small"></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Licitacion -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-file-earmark-text me-1"></i>Licitacion
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label asp-for="LicCode" class="form-label fw-semibold"></label>
                            <select asp-for="LicCode" asp-items="Model.Licitaciones" class="form-select" id="LicCode">
                                <option value="">-- Sin licitacion --</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="AssetDslic" class="form-label fw-semibold"></label>
                            <input asp-for="AssetDslic" class="form-control" type="date" id="AssetDslic" />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="AssetDelic" class="form-label fw-semibold"></label>
                            <input asp-for="AssetDelic" class="form-control" type="date" id="AssetDelic" />
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Historial -->
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-dark text-white fw-bold">
                    <i class="bi bi-clock-history me-1"></i>Historial
                </div>
                <div class="card-body">
                    <label asp-for="HistNotes" class="form-label fw-semibold"></label>
                    <input asp-for="HistNotes" class="form-control" placeholder="Ej: Ingreso inicial — instalacion en Hospital XXXX" />
                </div>
            </div>
        </div>
    </div>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-floppy me-1"></i>Guardar Activo
        </button>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.querySelectorAll('input[name="locationType"]').forEach(radio => {
            radio.addEventListener('change', function () {
                document.getElementById('secHospital').classList.add('d-none');
                document.getElementById('secBodega').classList.add('d-none');
                document.getElementById('secSede').classList.add('d-none');
                ['HospCode','HospDepCode','AssetHospPos','WareCode','WareRack','WareEstante','SiteCode']
                    .forEach(f => { const el = document.getElementById(f); if (el) el.value = ''; });
                if (this.value === 'hospital') document.getElementById('secHospital').classList.remove('d-none');
                if (this.value === 'bodega')   document.getElementById('secBodega').classList.remove('d-none');
                if (this.value === 'sede')     document.getElementById('secSede').classList.remove('d-none');
            });
        });

        document.getElementById('snInput').addEventListener('blur', function () {
            const sn = this.value.trim();
            const fb = document.getElementById('snFeedback');
            if (!sn) { fb.innerHTML = ''; return; }
            fetch(`/Engineering/CheckAssetSerial?sn=${encodeURIComponent(sn)}`)
                .then(r => r.json())
                .then(data => {
                    fb.innerHTML = data.exists
                        ? '<span class="text-danger"><i class="bi bi-exclamation-triangle-fill me-1"></i>Este serial ya esta registrado.</span>'
                        : '<span class="text-success"><i class="bi bi-check-circle-fill me-1"></i>Serial disponible.</span>';
                });
        });

        const licDates = @Html.Raw(ViewBag.LicitacionDates ?? "{}");
        document.getElementById('LicCode').addEventListener('change', function () {
            const data = licDates[this.value];
            document.getElementById('AssetDslic').value = data?.start ?? '';
            document.getElementById('AssetDelic').value = data?.end ?? '';
        });

        document.getElementById('netEnabledCheck').addEventListener('change', function () {
            document.getElementById('netDetails').classList.toggle('d-none', !this.checked);
        });

        document.getElementById('hospSelect').addEventListener('change', function () {
            const hospId = this.value;
            const depSelect = document.getElementById('hospDepSelect');
            depSelect.innerHTML = '<option value="">-- Cargando... --</option>';
            if (!hospId) { depSelect.innerHTML = '<option value="">-- Primero seleccione hospital --</option>'; return; }
            fetch(`/Equipment/GetHospDepartments?hospCode=${hospId}`)
                .then(r => r.json())
                .then(data => {
                    depSelect.innerHTML = '<option value="">-- Seleccione zona --</option>';
                    data.forEach(d => { depSelect.innerHTML += `<option value="${d.value}">${d.text}</option>`; });
                });
        });
    </script>
}
