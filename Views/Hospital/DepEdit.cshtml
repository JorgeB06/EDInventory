@model EDInventory.Models.ViewModels.HospitalDepartmentViewModel
@{ ViewData["Title"] = "Editar Departamento"; }

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0"><i class="bi bi-grid me-2"></i>Editar Departamento</h4>
    <a asp-action="Index" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left me-1"></i>Hospitales</a>
</div>

<div class="card shadow-sm" style="max-width:480px;">
    <div class="card-body">
        <form asp-action="DepEdit" method="post">
            @Html.AntiForgeryToken()
            <input asp-for="HospDepCode" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger small py-2"></div>
            <div class="mb-3">
                <label asp-for="HospDepName" class="form-label fw-semibold"></label>
                <input asp-for="HospDepName" class="form-control" />
                <span asp-validation-for="HospDepName" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="HospCode" class="form-label fw-semibold"></label>
                <select asp-for="HospCode" asp-items="Model.Hospitals" class="form-select">
                    <option value="">-- Seleccione hospital --</option>
                </select>
                <span asp-validation-for="HospCode" class="text-danger small"></span>
            </div>
            <div class="mb-4 form-check">
                <input asp-for="Active" class="form-check-input" type="checkbox" />
                <label asp-for="Active" class="form-check-label">Activo</label>
            </div>
            <button type="submit" class="btn btn-primary"><i class="bi bi-floppy me-1"></i>Guardar cambios</button>
        </form>
    </div>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
